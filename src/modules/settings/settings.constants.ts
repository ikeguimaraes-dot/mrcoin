/** Coins por R$1,00 × 100 — usado só como fallback quando uma organização é criada sem
 * coinsPerReal explícito. Não é mais uma "taxa da plataforma" editável: cada organização
 * tem a sua própria, a partir daqui. */
export const DEFAULT_COINS_PER_REAL_SCALED = 125;
